{
    "require": {
        "google/cloud-vision": "^1.6.0"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "vendor-dir": "vendor",
        "platform": {
            "php": "8.1"
        },
        "allow-plugins": {
            "google/cloud-tools": true
        },
        "home": ".composer"
    }
}